
# Imports

# Import packages for econometric analysis
import numpy as np
import pandas as pd
import statsmodels as sm

# Import plotting library
import matplotlib
import matplotlib.pyplot as plt
matplotlib.style.use('ggplot')



class WlsevEstimation(object):

    # 2. Estimate regression WLS-EV beta using Johnson (2016)
    # ------------------------------------------------------------------------------------------------------------
    # @params vol_est: single dataframe column with estimated daily realized volatility values
    # @params returns: single dataframe column with daily return values
    def estimate_wlf_ev(self, vol_est, returns):
        # import libraries
        import pandas as pd
        import statsmodels as sm

        self.variance.head()
        self.returns.head()




# Error Statistics
# --------------------------------------------------
